fastapi==0.115.0
uvicorn[standard]==0.30.6
anthropic>=0.40.0
python-multipart==0.0.26    # >=0.0.22 fixes CVE-2026-24486 (path traversal arbitrary file write)
aiofiles==24.1.0
jinja2==3.1.6               # >=3.1.5 fixes sandbox breakout CVEs
slowapi==0.1.9
psutil>=5.9.0               # CPU and disk usage reporting
# python-jose removed (unpatched CVEs, not currently used)
# Use pyjwt if JWT auth is needed in the future
